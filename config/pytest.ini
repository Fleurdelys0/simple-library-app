[pytest]
pythonpath = .

; Restrict test discovery to the tests/ directory
testpaths = ../tests

# Exclude integration tests by default
addopts = -m "not integration"

# Known markers
markers =
    integration: Integration tests that may require external services or be slow
    e2e: End-to-end tests that require a running server
